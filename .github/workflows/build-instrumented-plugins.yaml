name: Build Instrumented Plugin Images for E2E Coverage

on:
  workflow_call:
    inputs:
      workspace:
        description: 'Workspace to instrument'
        type: string
        required: true
      pr-number:
        description: 'PR number for tagging instrumented images with pr_{number}__{version}'
        type: string
        required: true
    secrets:
      GITHUB_TOKEN:
        required: true

  workflow_dispatch:
    inputs:
      workspace:
        description: 'Single workspace to instrument'
        type: string
        required: true
      pr-number:
        description: 'PR number (leave empty for manual testing)'
        type: string
        required: false
      force-rebuild:
        description: 'Force rebuild even if instrumented image already exists'
        type: boolean
        required: false
        default: false

concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  instrument:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Resolve production image from metadata
        id: meta
        env:
          WORKSPACE: ${{ inputs.workspace }}
          PR_NUMBER: ${{ inputs.pr-number }}
        run: |
          SOURCE_IMAGE=""
          PLUGIN_PATH=""
          PLUGIN_IMAGE_NAME=""
          COVERAGE_IMAGE=""
          IMAGE_TAG=""

          for meta_file in "workspaces/${WORKSPACE}/metadata"/*.yaml; do
            [ -e "$meta_file" ] || continue
            ROLE=$(yq -r '.spec.backstage.role // ""' "$meta_file")
            if [[ "$ROLE" != "frontend-plugin" ]]; then
              continue
            fi

            ARTIFACT=$(yq -r '.spec.dynamicArtifact // ""' "$meta_file")
            if [[ -z "$ARTIFACT" || "$ARTIFACT" == "null" || ! "$ARTIFACT" =~ ^oci:// ]]; then
              continue
            fi

            # Require the !plugin-path separator
            if [[ "$ARTIFACT" != *"!"* ]]; then
              echo "  Skipping $meta_file: dynamicArtifact missing '!' separator"
              continue
            fi

            # Parse: oci://registry/path/image:tag!plugin-path
            REF="${ARTIFACT#oci://}"
            PLUGIN_PATH="${REF##*!}"
            SOURCE_IMAGE_BASE="${REF%%!*}"

            IMAGE_NAME="${SOURCE_IMAGE_BASE%%:*}"
            IMAGE_TAG="${SOURCE_IMAGE_BASE##*:}"

            # Adjust tag for PR mode: replace bs_X.Y.Z__V.E.R with pr_NUMBER__V.E.R
            if [[ -n "$PR_NUMBER" ]]; then
              if [[ "$IMAGE_TAG" =~ ^bs_[0-9]+\.[0-9]+\.[0-9]+__(.+)$ ]]; then
                PLUGIN_VERSION="${BASH_REMATCH[1]}"
                IMAGE_TAG="pr_${PR_NUMBER}__${PLUGIN_VERSION}"
              else
                echo "  WARNING: Cannot parse plugin version from tag: $IMAGE_TAG"
                echo "  Expected format: bs_X.Y.Z__V.E.R"
                echo "  Using original tag"
              fi
            fi

            # Update SOURCE_IMAGE to use PR-tagged production image
            SOURCE_IMAGE="${IMAGE_NAME}:${IMAGE_TAG}"
            COVERAGE_IMAGE="${IMAGE_NAME}-coverage:${IMAGE_TAG}"
            PLUGIN_IMAGE_NAME=$(basename "$IMAGE_NAME")

            break
          done

          if [[ -z "$SOURCE_IMAGE" ]]; then
            echo "No frontend plugin with OCI dynamicArtifact found for ${WORKSPACE}"
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "source-image=$SOURCE_IMAGE" >> "$GITHUB_OUTPUT"
          echo "coverage-image=$COVERAGE_IMAGE" >> "$GITHUB_OUTPUT"
          echo "plugin-path=$PLUGIN_PATH" >> "$GITHUB_OUTPUT"
          echo "plugin-image-name=$PLUGIN_IMAGE_NAME" >> "$GITHUB_OUTPUT"
          echo "image-tag=$IMAGE_TAG" >> "$GITHUB_OUTPUT"
          echo "skip=false" >> "$GITHUB_OUTPUT"

          echo "  Workspace:       ${WORKSPACE}"
          echo "  Source image:     $SOURCE_IMAGE"
          echo "  Coverage image:   $COVERAGE_IMAGE"
          echo "  Plugin path:      $PLUGIN_PATH"
          if [[ -n "$PR_NUMBER" ]]; then
            echo "  PR mode:          pr_${PR_NUMBER}__*"
          fi

      - name: Check if instrumented image already exists
        if: steps.meta.outputs.skip != 'true'
        id: check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO_OWNER: ${{ github.repository_owner }}
          GITHUB_REPO: ${{ github.repository }}
          PLUGIN_IMAGE_NAME: ${{ steps.meta.outputs.plugin-image-name }}
          IMAGE_TAG: ${{ steps.meta.outputs.image-tag }}
          FORCE_REBUILD: ${{ inputs.force-rebuild }}
        run: |
          PACKAGE_PATH="${GITHUB_REPO}/${PLUGIN_IMAGE_NAME}-coverage"
          PACKAGE_PATH_ENCODED=$(echo "$PACKAGE_PATH" | sed 's|/|%2F|g')

          EXISTS=$(gh api "/orgs/${REPO_OWNER}/packages/container/${PACKAGE_PATH_ENCODED}/versions" \
            --jq "[.[] | select(.metadata.container.tags[] == \"$IMAGE_TAG\")] | length" 2>/dev/null || echo "0")

          if [[ "$EXISTS" -gt 0 ]] && [[ "$FORCE_REBUILD" != "true" ]]; then
            echo "Instrumented image already exists: ${PLUGIN_IMAGE_NAME}-coverage:${IMAGE_TAG}"
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "Instrumented image not found, will build"
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Log in to GitHub Container Registry
        if: steps.meta.outputs.skip != 'true' && steps.check.outputs.exists != 'true'
        env:
          REGISTRY_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
        run: echo "$REGISTRY_PASSWORD" | podman login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Setup Node.js
        if: steps.meta.outputs.skip != 'true' && steps.check.outputs.exists != 'true'
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 20

      - name: Instrument production image
        if: steps.meta.outputs.skip != 'true' && steps.check.outputs.exists != 'true'
        env:
          SOURCE_IMAGE: ${{ steps.meta.outputs.source-image }}
          COVERAGE_IMAGE: ${{ steps.meta.outputs.coverage-image }}
          PLUGIN_PATH: ${{ steps.meta.outputs.plugin-path }}
        run: ./scripts/instrument-plugin.sh "$SOURCE_IMAGE" "$COVERAGE_IMAGE" "$PLUGIN_PATH"

      - name: Push instrumented image
        if: steps.meta.outputs.skip != 'true' && steps.check.outputs.exists != 'true'
        env:
          COVERAGE_IMAGE: ${{ steps.meta.outputs.coverage-image }}
        run: |
          podman push "$COVERAGE_IMAGE"
          echo "Published: $COVERAGE_IMAGE"

      - name: Write job summary
        if: always() && steps.meta.outputs.skip != 'true'
        env:
          WORKSPACE: ${{ inputs.workspace }}
          SOURCE_IMAGE: ${{ steps.meta.outputs.source-image }}
          COVERAGE_IMAGE: ${{ steps.meta.outputs.coverage-image }}
          PLUGIN_PATH: ${{ steps.meta.outputs.plugin-path }}
          IMAGE_EXISTS: ${{ steps.check.outputs.exists }}
        run: |
          {
            echo "### Instrumented Plugin: ${WORKSPACE}"
            echo ""
            echo "| Field | Value |"
            echo "|-------|-------|"
            echo "| Source image | \`${SOURCE_IMAGE}\` |"
            echo "| Coverage image | \`${COVERAGE_IMAGE}\` |"
            echo "| Plugin path | \`${PLUGIN_PATH}\` |"
            if [[ "$IMAGE_EXISTS" == "true" ]]; then
              echo "| Status | Skipped (image already exists) |"
            else
              echo "| Status | Built and published |"
            fi
            echo ""
            echo "**Codecov flag:** \`e2e-${WORKSPACE}\`"
          } >> "$GITHUB_STEP_SUMMARY"
