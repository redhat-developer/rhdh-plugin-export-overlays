agent: agents/e2e-fix.md
model: opus
image: localhost:5050/rhdh-fullsend-code:local
policy: policies/e2e-fix.yaml

role: coder
slug: rhdh-e2e-fix

host_files:
  - src: env/gcp-vertex.env
    dest: /sandbox/workspace/.env.d/gcp-vertex.env
    expand: true
  - src: ${GOOGLE_APPLICATION_CREDENTIALS}
    dest: /tmp/.gcp-credentials.json
  - src: ${GCP_OIDC_TOKEN_FILE}
    dest: /tmp/.gcp-oidc-token
    optional: true
  - src: env/e2e-fix.env
    dest: /sandbox/workspace/.env.d/e2e-fix.env
    expand: true

skills:
  - skills/e2e-failure-analysis
  - skills/playwright-trace

post_script: scripts/post-e2e-fix.sh

validation_loop:
  script: scripts/validate-output-schema.sh
  max_iterations: 2

env:
  runner:
    GH_TOKEN: ${GH_TOKEN}
    REPO_FULL_NAME: redhat-developer/rhdh-plugin-export-overlays
    JIRA_URL: https://issues.redhat.com
    FULLSEND_OUTPUT_SCHEMA: ${FULLSEND_DIR}/schemas/e2e-fix-result.schema.json
    TARGET_REPO_DIR: ${FULLSEND_TARGET_REPO}
  sandbox:
    PROW_URL: "${PROW_URL}"
    SKILL_DIR: /sandbox/claude-config/skills/e2e-failure-analysis
    PLAYWRIGHT_BROWSERS_PATH: /opt/playwright-browsers
    GIT_AUTHOR_NAME: e2e-fix-agent
    GIT_AUTHOR_EMAIL: noreply@github.com
    GIT_COMMITTER_NAME: e2e-fix-agent
    GIT_COMMITTER_EMAIL: noreply@github.com

timeout_minutes: 30
