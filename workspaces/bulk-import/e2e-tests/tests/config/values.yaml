intelligentAssistant:
  enabled: false

extraVolumeMounts:
  - name: rbac-policy
    mountPath: /opt/app-root/src/rbac
extraVolumes:
  - name: rbac-policy
    configMap:
      defaultMode: 420
      name: rbac-policy
