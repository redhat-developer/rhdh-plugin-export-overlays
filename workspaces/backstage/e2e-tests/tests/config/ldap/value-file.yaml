upstream:
  backstage:
    appConfig:
      backend:
        auth:
          externalAccess:
            - type: static
              options:
                token: ldap-auth-e2e-token
                subject: ldap-auth-e2e
