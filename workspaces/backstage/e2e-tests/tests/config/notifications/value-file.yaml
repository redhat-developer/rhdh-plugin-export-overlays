intelligentAssistant:
  enabled: false

appConfig:
  backend:
    auth:
      externalAccess:
        - type: static
          options:
            token: test-token
            subject: test-subject
