/*
 * Stub component created as an overlay to fix CLI export error.
 * The CLI expects this component but it doesn't exist in the source.
 * This is a workaround for @red-hat-developer-hub/cli v1.10.7 bug.
 */
export const PodExecTerminal = () => null;

export default PodExecTerminal;
