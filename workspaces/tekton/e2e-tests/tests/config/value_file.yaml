# Lightspeed is being disabled due to https://redhat.atlassian.net/browse/RHDHBUGS-3145
global:
  lightspeed:
    enabled: false
upstream:
  # Run the hub as the namespace default SA so the in-cluster token matches RBAC
  serviceAccount:
    create: false
    name: default
  commonLabels:
    backstage.io/kubernetes-id: developer-hub
  backstage:
    # Helm replaces the whole initContainers list when set — mirror the chart default
    # (charts/backstage/values.yaml). MAX_ENTRY_SIZE is raised for large instrumented
    # __coverage plugin bundles (e.g. backstage-community-plugin-tekton).
    initContainers:
      - name: install-dynamic-plugins
        resources:
          requests:
            cpu: 250m
            memory: 256Mi
          limits:
            cpu: 1000m
            memory: 2.5Gi
            ephemeral-storage: 5Gi
        securityContext:
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
          runAsNonRoot: true
          seccompProfile:
            type: "RuntimeDefault"
        image: '{{ include "backstage.image" . }}'
        command:
          - ./install-dynamic-plugins.sh
          - /dynamic-plugins-root
        env:
          - name: NPM_CONFIG_USERCONFIG
            value: /opt/app-root/src/.npmrc.dynamic-plugins
          - name: MAX_ENTRY_SIZE
            value: "100000000"
          - name: CATALOG_INDEX_IMAGE
            value: "{{ .Values.global.catalogIndex.image.registry }}/{{ .Values.global.catalogIndex.image.repository }}:{{ .Values.global.catalogIndex.image.tag }}"
          - name: CATALOG_ENTITIES_EXTRACT_DIR
            value: "/extensions"
        imagePullPolicy: Always
        volumeMounts:
          - mountPath: /dynamic-plugins-root
            name: dynamic-plugins-root
          - mountPath: /opt/app-root/src/dynamic-plugins.yaml
            name: dynamic-plugins
            readOnly: true
            subPath: dynamic-plugins.yaml
          - mountPath: /opt/app-root/src/.npmrc.dynamic-plugins
            name: dynamic-plugins-npmrc
            readOnly: true
            subPath: .npmrc
          - mountPath: /opt/app-root/src/.config/containers
            name: dynamic-plugins-registry-auth
            readOnly: true
          - mountPath: /opt/app-root/src/.npm/_cacache
            name: npmcacache
          - name: extensions-catalog
            mountPath: /extensions
          - name: temp
            mountPath: /tmp
        workingDir: /opt/app-root/src
