# Presence of this file skips metadata auto-generation (which would enable
# auth-backend-module-keycloak-provider and inject AUTH_KEYCLOAK_* config).
# Catalog Keycloak comes from rhdh-e2e-test-utils auth/keycloak setup instead.
# Login uses OIDC from that same setup — not the Keycloak auth provider module.
plugins: []
