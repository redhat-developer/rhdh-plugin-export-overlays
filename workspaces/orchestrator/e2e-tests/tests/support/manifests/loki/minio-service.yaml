apiVersion: v1
kind: Service
metadata:
  name: ${MINIO_NAME}
  namespace: ${LOKI_NS}
  labels:
    app: ${MINIO_NAME}
spec:
  ports:
    - name: api
      port: 9000
      targetPort: 9000
  selector:
    app: ${MINIO_NAME}
