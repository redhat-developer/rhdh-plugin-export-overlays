apiVersion: v1
kind: Secret
metadata:
  name: rhdh-secrets
type: Opaque
stringData:
  VAULT_GITHUB_USER_TOKEN: $VAULT_GITHUB_USER_TOKEN
  # GITHUB_CLIENT_ID: $VAULT_GITHUB_CLIENT_ID
  # GITHUB_CLIENT_SECRET: $VAULT_GITHUB_CLIENT_SECRET
  # GH_USER_ID: $VAULT_GH_USER_ID
  # GH_USER_PASS: $VAULT_GH_USER_PASS
  # GH_2FA_SECRET: $VAULT_GH_2FA_SECRET
